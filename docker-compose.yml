services:
  # Base de données locale TimescaleDB
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    container_name: timescaledb
    environment:
      POSTGRES_DB: linkya_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      TZ: Europe/Paris
    ports:
      - "5432:5432"
    volumes:
      - timescaledb_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nilmia-network

  # Redis pour Celery
  redis:
    image: redis:7-alpine
    container_name: redis
    environment:
      TZ: Europe/Paris
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - nilmia-network

  # Service de synchronisation - Worker Celery
  sync-worker:
    build:
      context: ./sync-service
      dockerfile: Dockerfile
    container_name: sync-worker
    command: celery -A src.tasks.celery_app worker --loglevel=info
    environment:
      TZ: Europe/Paris
    env_file:
      - .env
    depends_on:
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./sync-service/src:/app/src
    networks:
      - nilmia-network
    restart: unless-stopped

  # Celery Beat - Planificateur de tâches (initialise aussi la DB au démarrage)
  sync-beat:
    build:
      context: ./sync-service
      dockerfile: Dockerfile
    container_name: sync-beat
    command: /app/scripts/entrypoint-beat.sh
    environment:
      TZ: Europe/Paris
    env_file:
      - .env
    depends_on:
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./sync-service/src:/app/src
    networks:
      - nilmia-network
    restart: unless-stopped

  # Service NILM - Worker Celery
  nilm-worker:
    build:
      context: ./nilm-service
      dockerfile: Dockerfile
    container_name: nilm-worker
    command: celery -A src.tasks.celery_app worker --loglevel=info --pool=solo -Q nilm
    runtime: nvidia
    environment:
      TZ: Europe/Paris
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
      C_FORCE_ROOT: "true"  # Désactive le warning Celery root (container Docker isolé)
      NILM_MODEL_TYPE: "gru"  # Type de modèle: 'gru' (défaut) ou 'lstm'
    env_file:
      - .env
    depends_on:
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
      sync-worker:
        condition: service_started
    volumes:
      - ./nilm-service/src:/app/src
      - ./.data/models:/app/models
      - nilm_uv_cache:/root/.cache/uv
    networks:
      - nilmia-network
    restart: unless-stopped

  # Celery Beat pour NILM (initialise aussi les tables NILM au démarrage)
  nilm-beat:
    build:
      context: ./nilm-service
      dockerfile: Dockerfile
    container_name: nilm-beat
    command: /app/scripts/entrypoint-beat.sh
    environment:
      TZ: Europe/Paris
      C_FORCE_ROOT: "true"  # Désactive le warning Celery root (container Docker isolé)
      CUDA_VISIBLE_DEVICES: ""  # Pas de GPU pour beat (planificateur uniquement)
    env_file:
      - .env
    depends_on:
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
      sync-beat:
        condition: service_started
    volumes:
      - ./nilm-service/src:/app/src
      - ./.data/models:/app/models
    networks:
      - nilmia-network
    restart: unless-stopped

  # Backend API FastAPI
  backend:
    build:
      context: ./backend-service
      dockerfile: Dockerfile
    container_name: backend
    environment:
      TZ: Europe/Paris
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      timescaledb:
        condition: service_healthy
      sync-worker:
        condition: service_started
    volumes:
      - ./backend-service/src:/app/src
      - ./.data/models:/app/models
    networks:
      - nilmia-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 5s
      timeout: 10s
      retries: 3

  # React dev server (hot reload)
  react-dev:
    build:
      context: ./frontend-service
      dockerfile: Dockerfile.dev
    container_name: react-dev
    command: npm start
    environment:
      TZ: Europe/Paris
      REACT_APP_API_URL: ${REACT_APP_API_URL}
      REACT_APP_WS_URL: ${REACT_APP_WS_URL}
      WDS_SOCKET_PORT: 0
    volumes:
      - ./frontend-service/src:/app/src
      - ./frontend-service/public:/app/public
      - ./frontend-service/package.json:/app/package.json
      - /app/node_modules
    networks:
      - nilmia-network
    restart: unless-stopped
    stdin_open: true
    tty: true

  # Nginx reverse proxy
  frontend:
    build:
      context: ./frontend-service
      dockerfile: Dockerfile.nginx
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
      react-dev:
        condition: service_started
    networks:
      - nilmia-network
    restart: unless-stopped

  # pgweb - Interface web PostgreSQL/TimescaleDB
  pgweb:
    image: sosedoff/pgweb:latest
    container_name: pgweb
    environment:
      PGWEB_DATABASE_URL: postgres://${LOCAL_DB_USER}:${LOCAL_DB_PASSWORD}@${LOCAL_DB_HOST}:${LOCAL_DB_PORT}/${LOCAL_DB_NAME}?sslmode=disable
      TZ: Europe/Paris
    ports:
      - "8081:8081"
    networks:
      - nilmia-network
    depends_on:
      timescaledb:
        condition: service_healthy
    restart: unless-stopped

volumes:
  timescaledb_data:
  redis_data:
  nilm_uv_cache:

networks:
  nilmia-network:
    driver: bridge
    enable_ipv6: false
    driver_opts:
      com.docker.network.enable_ipv6: "false"
